import React from 'react';
import s from './Footer.module.scss';

const Footer = () => {
    return (
        <footer className={s.root}>
            Made by Tim Lobanov
        </footer>
    );
};

export default Footer;